import re

class GridCoord(object):
    """
    This object takes a two character string and converts and stores it
    as a (column, row) tuple that corresponds to a square on the board.
    ([a-j][1-10])
    """
    def __init__(self):
        pass
    